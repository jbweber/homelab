[defaults]
inventory = ./inventory.ini

# Pattern: Users should create their Ansible vault file at ~/.homelab.vault (not in the repo).
# Vault should include SSH user and sudo password variables, kept outside the project repo for security.
